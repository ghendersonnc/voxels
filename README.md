# voxels
hello

```sh
$ git clone https://github.com/ghendersonnc/voxels.git
$ mkdir build && cd build

# add -DCMAKE_BUILD_TYPE=Release if you want
$ cmake ..
$ cmake --build .
$ ./Debug/voxels # (.exe if Windows, Release directory if using Release build type)
```